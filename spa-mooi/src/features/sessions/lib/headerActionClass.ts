/**
 * Shared box of the workspace header actions: icon-only on phones, and a minimum width from `sm`
 * so a label swap (Deploy → Deploying…, Merge → Merged) barely moves its neighbours.
 */
export const HEADER_ACTION_CLASS = 'relative w-10 px-0 sm:w-auto sm:min-w-[7.5rem] sm:px-3.5';
